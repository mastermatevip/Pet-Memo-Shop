# Copy to .env.local for local dev, or set in Coolify → Environment Variables
NODE_ENV=production
PORT=3000

# Public site URL (optional — brand.ts defaults to https://petmemoshop.com)
# NEXT_PUBLIC_SITE_URL=https://petmemoshop.com

# Admin CMS (required for /admin)
ADMIN_PASSWORD=change-me-to-a-strong-password
ADMIN_SECRET=change-me-to-a-long-random-string
